.Side-bar
  .Side-bar__user-name
    .top-items
      .top-items__name
        .top-items
          = current_user.name
          masa
          .top-items__icons
            = link_to new_group_path(current_user), class: "icon-btn" do
              = icon('far', 'edit')
            = link_to edit_user_path(current_user), class: "icon-btn" do
              = icon('fas', 'cog')
  .Side-bar__group-list
    - current_user.groups.each do |group|
      .Group
        = link_to '#' do
          .group-list__name
            = group.name
          .group-list__message
            メッセージはまだありません